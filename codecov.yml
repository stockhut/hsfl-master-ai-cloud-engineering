coverage:
  status:
    project:
      default:
        informational: true
    patch:
        informational: true
flags:
  auth:
    paths:
      - authentication
  recipe:
    paths:
      - recipe
  common:
    paths:
      - common
  loadbalancer:
    paths:
      - reverse-proxy
  reverseproxy:
    paths:
      - load-balancer

flag_management:
  default_rules:
    carryforward: true
